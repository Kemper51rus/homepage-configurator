diff --git a/next.config.js b/next.config.js
index ea49bf5..7426606 100644
--- a/next.config.js
+++ b/next.config.js
@@ -1,9 +1,19 @@
 const { i18n } = require("./next-i18next.config");
 
+const allowedDevOrigins = (process.env.HOMEPAGE_ALLOWED_DEV_ORIGINS ?? "")
+  .split(",")
+  .map((origin) => origin.trim())
+  .filter(Boolean);
+
 /** @type {import('next').NextConfig} */
 const nextConfig = {
   reactStrictMode: true,
   output: "standalone",
+  outputFileTracingRoot: __dirname,
+  env: {
+    HOMEPAGE_BROWSER_EDITOR: process.env.HOMEPAGE_BROWSER_EDITOR ?? "",
+  },
+  ...(allowedDevOrigins.length ? { allowedDevOrigins } : {}),
   images: {
     remotePatterns: [
       {
diff --git a/src/components/bookmarks/group.jsx b/src/components/bookmarks/group.jsx
index 88d4d7e..af3ee26 100644
--- a/src/components/bookmarks/group.jsx
+++ b/src/components/bookmarks/group.jsx
@@ -5,6 +5,7 @@ import ErrorBoundary from "components/errorboundry";
 import ResolvedIcon from "components/resolvedicon";
 import { useEffect, useRef } from "react";
 import { MdKeyboardArrowDown } from "react-icons/md";
+import { useConfigEditor, useEditableGroupHeader } from "mods/browser-editor/components/editor";
 
 export default function BookmarksGroup({
   bookmarks,
@@ -14,15 +15,23 @@ export default function BookmarksGroup({
   bookmarksStyle,
   maxGroupColumns,
 }) {
+  const { editMode } = useConfigEditor();
+  const groupHeaderProps = useEditableGroupHeader("bookmarks", bookmarks.name, layout);
+  const headerHidden = layout?.header === false;
+  const showHeader = !headerHidden || editMode;
   const panel = useRef();
-
   useEffect(() => {
     if (layout?.initiallyCollapsed ?? groupsInitiallyCollapsed) panel.current.style.height = `0`;
   }, [layout, groupsInitiallyCollapsed]);
 
+  if (!editMode && (bookmarks.bookmarks?.length ?? 0) === 0) {
+    return null;
+  }
+
   return (
     <div
       key={bookmarks.name}
+      data-editor-group-name={bookmarks.name}
       className={classNames(
         "bookmark-group flex-1 overflow-hidden",
         layout?.style === "row" ? "basis-full" : "basis-full md:basis-1/4 lg:basis-1/5 xl:basis-1/6",
@@ -35,8 +44,17 @@ export default function BookmarksGroup({
       <Disclosure defaultOpen={!(layout?.initiallyCollapsed ?? groupsInitiallyCollapsed)}>
         {({ open }) => (
           <>
-            {layout?.header !== false && (
-              <Disclosure.Button disabled={disableCollapse} className="flex w-full select-none items-center group">
+            {showHeader && (
+              <Disclosure.Button
+                disabled={editMode ? false : disableCollapse}
+                {...groupHeaderProps}
+                className={classNames(
+                  "flex w-full select-none items-center group transition-colors",
+                  editMode &&
+                    "relative z-[61] cursor-grab rounded-md border border-theme-400/70 bg-theme-100/10 px-2 py-1 text-theme-800 hover:border-theme-500/80 hover:bg-theme-200/40 hover:text-theme-900 active:cursor-grabbing dark:border-white/25 dark:bg-white/5 dark:text-theme-100 dark:hover:border-white/40 dark:hover:bg-white/10",
+                  editMode && headerHidden && "opacity-55 blur-[0.35px] saturate-75",
+                )}
+              >
                 {layout?.icon && (
                   <div className="shrink-0 mr-2 w-7 h-7 bookmark-group-icon">
                     <ResolvedIcon icon={layout.icon} />
@@ -73,7 +91,12 @@ export default function BookmarksGroup({
             >
               <Disclosure.Panel className="transition-all overflow-hidden duration-300 ease-out" ref={panel} static>
                 <ErrorBoundary>
-                  <List bookmarks={bookmarks.bookmarks} layout={layout} bookmarksStyle={bookmarksStyle} />
+                  <List
+                    groupName={bookmarks.name}
+                    bookmarks={bookmarks.bookmarks}
+                    layout={layout}
+                    bookmarksStyle={bookmarksStyle}
+                  />
                 </ErrorBoundary>
               </Disclosure.Panel>
             </Transition>
diff --git a/src/components/bookmarks/item.jsx b/src/components/bookmarks/item.jsx
index 7ea16bb..7fd57bb 100644
--- a/src/components/bookmarks/item.jsx
+++ b/src/components/bookmarks/item.jsx
@@ -1,24 +1,88 @@
 import classNames from "classnames";
+import { useEditableItem } from "mods/browser-editor/components/editor";
 import ResolvedIcon from "components/resolvedicon";
-import { useContext } from "react";
+import { useContext, useEffect, useRef, useState } from "react";
 import { SettingsContext } from "utils/contexts/settings";
 
+function bookmarkDescription(bookmark) {
+  if (bookmark.description) {
+    return bookmark.description;
+  }
+
+  try {
+    return new URL(bookmark.href).hostname;
+  } catch {
+    return bookmark.href ?? "";
+  }
+}
+
 export default function Item({ bookmark, iconOnly = false }) {
-  const description = bookmark.description ?? new URL(bookmark.href).hostname;
+  const description = bookmarkDescription(bookmark);
+  const { editMode, itemProps } = useEditableItem(
+    "bookmarks",
+    bookmark.groupName,
+    bookmark.name,
+    bookmark,
+    bookmark.itemIndex,
+  );
   const { settings } = useContext(SettingsContext);
+  const wrapperRef = useRef(null);
+  const bookmarkRef = useRef(null);
+  const [overlayStyle, setOverlayStyle] = useState({ borderRadius: "inherit" });
+
+  useEffect(() => {
+    if (!editMode || !wrapperRef.current || !bookmarkRef.current) {
+      return undefined;
+    }
+
+    let animationFrame = 0;
+    let resizeObserver = null;
+    const updateOverlayStyle = () => {
+      if (!wrapperRef.current || !bookmarkRef.current) {
+        return;
+      }
+
+      const wrapperRect = wrapperRef.current.getBoundingClientRect();
+      const bookmarkRect = bookmarkRef.current.getBoundingClientRect();
+      setOverlayStyle({
+        borderRadius: window.getComputedStyle(bookmarkRef.current).borderRadius || "inherit",
+        height: `${bookmarkRect.height}px`,
+        left: `${bookmarkRect.left - wrapperRect.left}px`,
+        top: `${bookmarkRect.top - wrapperRect.top}px`,
+        width: `${bookmarkRect.width}px`,
+      });
+    };
+
+    animationFrame = window.requestAnimationFrame(updateOverlayStyle);
+    window.addEventListener("resize", updateOverlayStyle);
+
+    if (window.ResizeObserver) {
+      resizeObserver = new window.ResizeObserver(updateOverlayStyle);
+      resizeObserver.observe(wrapperRef.current);
+      resizeObserver.observe(bookmarkRef.current);
+    }
+
+    return () => {
+      window.cancelAnimationFrame(animationFrame);
+      window.removeEventListener("resize", updateOverlayStyle);
+      resizeObserver?.disconnect();
+    };
+  }, [editMode, bookmark.name, iconOnly]);
 
   return (
     <li
+      ref={wrapperRef}
       key={bookmark.name}
       id={bookmark.id}
-      className={classNames("bookmark", iconOnly && "grid")}
+      className={classNames("bookmark relative", iconOnly && "grid")}
       data-name={bookmark.name}
     >
       <a
-        href={bookmark.href}
+        ref={bookmarkRef}
+        href={editMode ? undefined : bookmark.href}
         title={bookmark.name}
         rel="noreferrer"
-        target={bookmark.target ?? settings.target ?? "_blank"}
+        target={editMode ? undefined : bookmark.target ?? settings.target ?? "_blank"}
         className={classNames(
           settings.cardBlur !== undefined && `backdrop-blur${settings.cardBlur.length ? "-" : ""}${settings.cardBlur}`,
           "text-left cursor-pointer transition-all rounded-md font-medium text-theme-700 dark:text-theme-200 dark:hover:text-theme-300 shadow-md shadow-theme-900/10 dark:shadow-theme-900/20 bg-theme-100/20 hover:bg-theme-300/20 dark:bg-white/5 dark:hover:bg-white/10",
@@ -53,6 +117,15 @@ export default function Item({ bookmark, iconOnly = false }) {
           </div>
         )}
       </a>
+      {editMode && (
+        <button
+          type="button"
+          {...itemProps}
+          style={overlayStyle}
+          className="absolute z-[70] pointer-events-auto cursor-move border-2 border-dashed border-theme-400/70 bg-transparent text-left dark:border-white/25"
+          aria-label={`Edit ${bookmark.name}`}
+        />
+      )}
     </li>
   );
 }
diff --git a/src/components/bookmarks/list.jsx b/src/components/bookmarks/list.jsx
index f24794d..23c8c3b 100644
--- a/src/components/bookmarks/list.jsx
+++ b/src/components/bookmarks/list.jsx
@@ -1,9 +1,10 @@
 import classNames from "classnames";
 import Item from "components/bookmarks/item";
+import { EditorAddTile } from "mods/browser-editor/components/editor";
 
 import { columnMap } from "../../utils/layout/columns";
 
-export default function List({ bookmarks, layout, bookmarksStyle }) {
+export default function List({ groupName, bookmarks, layout, bookmarksStyle }) {
   let classes = layout?.style === "row" ? `grid ${columnMap[layout?.columns]} gap-x-2` : "flex flex-col bookmark-list";
   const style = {};
   if (layout?.iconsOnly || bookmarksStyle === "icons") {
@@ -12,13 +13,23 @@ export default function List({ bookmarks, layout, bookmarksStyle }) {
   }
   return (
     <ul className={classNames(classes, "mb-2", layout?.header === false ? "" : "mt-3")} style={style}>
-      {bookmarks.map((bookmark) => (
+      {bookmarks.map((bookmark, index) => (
         <Item
           key={`${bookmark.name}-${bookmark.href}`}
-          bookmark={bookmark}
+          bookmark={{ ...bookmark, groupName, itemIndex: index }}
           iconOnly={layout?.iconsOnly || bookmarksStyle === "icons"}
         />
       ))}
+      <EditorAddTile
+        type="bookmarks"
+        groupName={groupName}
+        label="Add"
+        wrapperClassName={classNames("bookmark", (layout?.iconsOnly || bookmarksStyle === "icons") && "grid")}
+        className={classNames(
+          "flex items-center justify-center rounded-md border-2 border-dashed border-theme-400/70 bg-theme-100/20 p-3 text-sm font-medium text-theme-700 shadow-md shadow-theme-900/10 hover:bg-theme-300/20 dark:border-white/20 dark:bg-white/5 dark:text-theme-200 dark:hover:bg-white/10",
+          layout?.iconsOnly || bookmarksStyle === "icons" ? "h-[60px] w-[60px]" : "mb-3 min-h-12 w-full",
+        )}
+      />
     </ul>
   );
 }
diff --git a/src/components/services/group.jsx b/src/components/services/group.jsx
index 403f606..fcdf4b3 100644
--- a/src/components/services/group.jsx
+++ b/src/components/services/group.jsx
@@ -4,6 +4,7 @@ import ResolvedIcon from "components/resolvedicon";
 import List from "components/services/list";
 import { useEffect, useRef } from "react";
 import { MdKeyboardArrowDown } from "react-icons/md";
+import { useConfigEditor, useEditableGroupHeader, useGroupInsideDropTarget } from "mods/browser-editor/components/editor";
 
 import { columnMap } from "../../utils/layout/columns";
 
@@ -16,18 +17,52 @@ export default function ServicesGroup({
   groupsInitiallyCollapsed,
   isSubgroup,
 }) {
+  const { editMode } = useConfigEditor();
+  const groupInsideDropProps = useGroupInsideDropTarget("services", group.name, editMode);
+  const groupHeaderProps = useEditableGroupHeader("services", group.name, layout);
+  const headerHidden = layout?.header === false;
+  const showHeader = !headerHidden || editMode;
   const panel = useRef();
-
   useEffect(() => {
     if (layout?.initiallyCollapsed ?? groupsInitiallyCollapsed) panel.current.style.height = `0`;
   }, [layout, groupsInitiallyCollapsed]);
 
+  const subgroupsFromApi = group.groups ?? [];
+  const subgroupNamesFromApi = new Set(subgroupsFromApi.map((subgroup) => subgroup.name));
+  const layoutSubgroups = Object.entries(layout ?? {})
+    .filter(
+      ([key, value]) =>
+        ![
+          "style",
+          "columns",
+          "header",
+          "tab",
+          "icon",
+          "initiallyCollapsed",
+          "useEqualHeights",
+          "alignRowHeights",
+        ].includes(key),
+    )
+    .filter(([, value]) => value && typeof value === "object" && !Array.isArray(value))
+    .map(([key]) => ({ name: key, services: [], groups: [] }))
+    .filter((subgroup) => !subgroupNamesFromApi.has(subgroup.name));
+  const visibleSubgroups = [...subgroupsFromApi, ...(editMode ? layoutSubgroups : [])];
+  const isEmptyGroup = (group.services?.length ?? 0) === 0 && visibleSubgroups.length === 0;
+
+  if (!editMode && isEmptyGroup) {
+    return null;
+  }
+
   let groupPadding = layout?.header === false ? "px-1" : "p-1 pb-0";
   if (isSubgroup) groupPadding = "";
 
   return (
     <div
       key={group.name}
+      data-editor-service-group="true"
+      data-editor-group-name={group.name}
+      data-editor-align-row-heights={layout?.alignRowHeights === false ? "false" : "true"}
+      {...groupInsideDropProps}
       className={classNames(
         "services-group flex-1",
         layout?.style === "row" ? "basis-full" : "basis-full md:basis-1/2 lg:basis-1/3 xl:basis-1/4",
@@ -39,8 +74,17 @@ export default function ServicesGroup({
       <Disclosure defaultOpen={!(layout?.initiallyCollapsed ?? groupsInitiallyCollapsed)}>
         {({ open }) => (
           <>
-            {layout?.header !== false && (
-              <Disclosure.Button disabled={disableCollapse} className="flex w-full select-none items-center group">
+            {showHeader && (
+              <Disclosure.Button
+                disabled={editMode ? false : disableCollapse}
+                {...groupHeaderProps}
+                className={classNames(
+                  "flex w-full select-none items-center group transition-colors",
+                  editMode &&
+                    "relative z-[61] cursor-grab rounded-md border border-theme-400/70 bg-theme-100/10 px-2 py-1 text-theme-800 hover:border-theme-500/80 hover:bg-theme-200/40 hover:text-theme-900 active:cursor-grabbing dark:border-white/25 dark:bg-white/5 dark:text-theme-100 dark:hover:border-white/40 dark:hover:bg-white/10",
+                  editMode && headerHidden && "opacity-55 blur-[0.35px] saturate-75",
+                )}
+              >
                 {layout?.icon && (
                   <div className="shrink-0 mr-2 w-7 h-7 service-group-icon">
                     <ResolvedIcon icon={layout.icon} />
@@ -86,13 +130,13 @@ export default function ServicesGroup({
                   useEqualHeights={useEqualHeights}
                   header={layout?.header !== false}
                 />
-                {group.groups?.length > 0 && (
+                {visibleSubgroups.length > 0 && (
                   <div
                     className={`grid ${
                       layout?.style === "row" ? `grid ${columnMap[layout?.columns]} gap-x-2` : "flex flex-col"
                     } gap-2`}
                   >
-                    {group.groups.map((subgroup) => (
+                    {visibleSubgroups.map((subgroup) => (
                       <ServicesGroup
                         key={subgroup.name}
                         group={subgroup}
diff --git a/src/components/services/item.jsx b/src/components/services/item.jsx
index c23e16a..9c53cc0 100644
--- a/src/components/services/item.jsx
+++ b/src/components/services/item.jsx
@@ -1,5 +1,6 @@
 import classNames from "classnames";
 import ResolvedIcon from "components/resolvedicon";
+import { useEditableItem } from "mods/browser-editor/components/editor";
 import { useContext, useState } from "react";
 import { SettingsContext } from "utils/contexts/settings";
 import Docker from "widgets/docker/component";
@@ -13,8 +14,9 @@ import SiteMonitor from "./site-monitor";
 import Status from "./status";
 import Widget from "./widget";
 
-export default function Item({ service, groupName, useEqualHeights }) {
+export default function Item({ service, groupName, useEqualHeights, itemIndex }) {
   const hasLink = service.href && service.href !== "#";
+  const { editMode, itemProps } = useEditableItem("services", groupName, service.name, service, itemIndex);
   const { settings } = useContext(SettingsContext);
   const showStats = service.showStats === false ? false : settings.showStats;
   const statusStyle = service.statusStyle !== undefined ? service.statusStyle : settings.statusStyle;
@@ -188,6 +190,16 @@ export default function Item({ service, groupName, useEqualHeights }) {
         {service.widgets.map((widget) => (
           <Widget widget={widget} service={service} key={widget.index} />
         ))}
+
+        {editMode && (
+          <button
+            type="button"
+            {...itemProps}
+            style={{ borderRadius: "inherit", inset: "2px" }}
+            className="absolute z-[70] pointer-events-auto cursor-move border-2 border-dashed border-theme-400/70 bg-transparent text-left dark:border-white/25"
+            aria-label={`Edit ${service.name}`}
+          />
+        )}
       </div>
     </li>
   );
diff --git a/src/components/services/list.jsx b/src/components/services/list.jsx
index 1acf94d..531ba65 100644
--- a/src/components/services/list.jsx
+++ b/src/components/services/list.jsx
@@ -1,25 +1,37 @@
 import classNames from "classnames";
 import Item from "components/services/item";
+import { EditorAddTile } from "mods/browser-editor/components/editor";
 
 import { columnMap } from "../../utils/layout/columns";
 
 export default function List({ groupName, services, layout, useEqualHeights, header }) {
+  const isRowLayout = layout?.style === "row";
+
   return (
     <ul
+      data-editor-service-list={groupName}
       className={classNames(
-        layout?.style === "row" ? `grid ${columnMap[layout?.columns]} gap-x-2` : "flex flex-col",
+        isRowLayout ? `grid ${columnMap[layout?.columns]} gap-x-2` : "flex flex-col",
         header ? "mt-3" : "",
         "services-list",
       )}
     >
-      {services.map((service) => (
+      {services.map((service, index) => (
         <Item
           key={[service.container, service.app, service.name].filter((s) => s).join("-")}
           service={service}
           groupName={groupName}
+          itemIndex={index}
           useEqualHeights={layout?.useEqualHeights ?? useEqualHeights}
         />
       ))}
+      <EditorAddTile
+        type="services"
+        groupName={groupName}
+        label="Add service"
+        wrapperClassName="service"
+        className="mb-2 flex min-h-16 w-full items-center justify-center rounded-md border-2 border-dashed border-theme-400/70 bg-theme-100/20 p-3 text-sm font-medium text-theme-700 shadow-md shadow-theme-900/10 hover:bg-theme-300/20 dark:border-white/20 dark:bg-white/5 dark:text-theme-200 dark:hover:bg-white/10"
+      />
     </ul>
   );
 }
diff --git a/src/components/widgets/datetime/datetime.jsx b/src/components/widgets/datetime/datetime.jsx
index f0bae0b..a868fad 100644
--- a/src/components/widgets/datetime/datetime.jsx
+++ b/src/components/widgets/datetime/datetime.jsx
@@ -1,5 +1,5 @@
 import { useTranslation } from "next-i18next/pages";
-import { useEffect, useState } from "react";
+import { useEffect, useState, useMemo } from "react";
 
 import Container from "../widget/container";
 import Raw from "../widget/raw";
@@ -17,25 +17,133 @@ const textSizes = {
 
 export default function DateTime({ options }) {
   const { text_size: textSize, locale, format } = options;
+  const clockStyle = options?.clockStyle ?? {};
   const { i18n } = useTranslation();
-  const [date, setDate] = useState("");
   const dateLocale = locale ?? i18n.language;
 
+  const [time, setTime] = useState(() => new Date());
+
   useEffect(() => {
-    const dateFormat = new Intl.DateTimeFormat(dateLocale, { ...format });
-    setDate(dateFormat.format(new Date()));
     const interval = setInterval(() => {
-      setDate(dateFormat.format(new Date()));
+      setTime(new Date());
     }, 1000);
     return () => clearInterval(interval);
-  }, [date, setDate, dateLocale, format]);
+  }, []);
+
+  const clockType = clockStyle.type ?? "digital-one-line";
+
+  // Formatted date and time strings
+  const formattedTime = useMemo(() => {
+    try {
+      return new Intl.DateTimeFormat(dateLocale, {
+        hour: "numeric",
+        minute: "numeric",
+        second: "numeric",
+        hour12: false,
+      }).format(time);
+    } catch {
+      return time.toLocaleTimeString(dateLocale);
+    }
+  }, [time, dateLocale]);
+
+  const formattedDate = useMemo(() => {
+    try {
+      return new Intl.DateTimeFormat(dateLocale, {
+        dateStyle: "medium",
+      }).format(time);
+    } catch {
+      return time.toLocaleDateString(dateLocale);
+    }
+  }, [time, dateLocale]);
+
+  const defaultFormatted = useMemo(() => {
+    try {
+      const dateFormat = new Intl.DateTimeFormat(dateLocale, { ...format });
+      return dateFormat.format(time);
+    } catch {
+      return time.toLocaleString(dateLocale);
+    }
+  }, [time, dateLocale, format]);
+
+  // SVG Analog clock calculations
+  const hourDeg = (time.getHours() % 12) * 30 + time.getMinutes() * 0.5;
+  const minuteDeg = time.getMinutes() * 6 + time.getSeconds() * 0.1;
+  const secondDeg = time.getSeconds() * 6;
+
+  const align = clockStyle.align ?? "right";
+  const noBackground = clockStyle.noBackground ?? false;
+
+  const rowAlignClass = align === "left" ? "justify-start" : align === "center" ? "justify-center" : "justify-end";
+  const colAlignClass = align === "left" ? "items-start text-left" : align === "center" ? "items-center text-center" : "items-end text-right";
+
+  // Layout selection
+  const renderClockContent = () => {
+    const textColor = clockStyle.color || "";
+    const textStyle = {
+      color: textColor ? textColor : undefined,
+      fontFamily: clockStyle.fontFamily || undefined,
+      fontSize: clockStyle.fontSize || undefined,
+    };
+
+    switch (clockType) {
+      case "only-time":
+        return (
+          <span className="tabular-nums" style={textStyle}>
+            {formattedTime}
+          </span>
+        );
+      case "only-date":
+        return (
+          <span style={textStyle}>
+            {formattedDate}
+          </span>
+        );
+      case "digital-two-lines-date-time":
+        return (
+          <div className={`flex flex-col leading-tight ${colAlignClass}`}>
+            <span style={{ ...textStyle, fontSize: clockStyle.fontSize ? `calc(${clockStyle.fontSize} * 0.75)` : undefined }} className="opacity-80">
+              {formattedDate}
+            </span>
+            <span style={textStyle} className="font-semibold tabular-nums">
+              {formattedTime}
+            </span>
+          </div>
+        );
+      case "digital-two-lines-time-date":
+        return (
+          <div className={`flex flex-col leading-tight ${colAlignClass}`}>
+            <span style={textStyle} className="font-semibold tabular-nums">
+              {formattedTime}
+            </span>
+            <span style={{ ...textStyle, fontSize: clockStyle.fontSize ? `calc(${clockStyle.fontSize} * 0.75)` : undefined }} className="opacity-80">
+              {formattedDate}
+            </span>
+          </div>
+        );
+      case "digital-one-line":
+      default:
+        // Use standard inline format or custom format
+        const defaultStyle = {
+          color: textColor ? textColor : undefined,
+          fontFamily: clockStyle.fontFamily || undefined,
+          fontSize: clockStyle.fontSize || undefined,
+        };
+        return (
+          <span className="tabular-nums" style={defaultStyle}>
+            {clockStyle.type && clockStyle.type !== "analog" ? `${formattedDate}, ${formattedTime}` : defaultFormatted}
+          </span>
+        );
+    }
+  };
+
+  const additionalClassNames = `information-widget-datetime ${noBackground ? "!bg-transparent !shadow-none !border-none !p-0 !backdrop-blur-none" : ""}`;
 
   return (
-    <Container options={options} additionalClassNames="information-widget-datetime">
+    <Container options={options} additionalClassNames={additionalClassNames}>
       <Raw>
-        <div className="flex flex-row items-center grow justify-end">
-          <span className={`text-theme-800 dark:text-theme-200 tabular-nums ${textSizes[textSize || "lg"]}`}>
-            {date}
+        <div className={`flex flex-row items-center grow ${rowAlignClass}`}>
+          <span className={`text-theme-800 dark:text-theme-200 ${textSizes[textSize || "lg"]}`}>
+            {renderClockContent()}
           </span>
         </div>
       </Raw>
diff --git a/src/components/widgets/openweathermap/weather.jsx b/src/components/widgets/openweathermap/weather.jsx
index bcca693..e49ea60 100644
--- a/src/components/widgets/openweathermap/weather.jsx
+++ b/src/components/widgets/openweathermap/weather.jsx
@@ -52,11 +52,41 @@ function Widget({ options }) {
     weatherStyle.hideBackground ? "weather-hide-background" : ""
   ].filter(Boolean).join(" ");
 
-  const textStyle = {
-    fontSize: weatherStyle.fontSize || undefined,
+  const baseTextStyle = {
+    fontFamily: weatherStyle.fontFamily || undefined,
     color: weatherStyle.textColor || undefined
   };
 
+  const tempStyle = {
+    ...baseTextStyle,
+    fontSize: weatherStyle.fontSize
+      ? (layout === "custom"
+          ? `calc(${weatherStyle.fontSize} * 1.4)`
+          : layout === "vertical"
+            ? `calc(${weatherStyle.fontSize} * 1.25)`
+            : weatherStyle.fontSize)
+      : undefined
+  };
+
+  const descStyle = {
+    ...baseTextStyle,
+    fontSize: weatherStyle.fontSize ? `calc(${weatherStyle.fontSize} * 0.85)` : undefined
+  };
+
+  const cityStyle = {
+    ...baseTextStyle,
+    fontSize: weatherStyle.fontSize
+      ? (layout === "vertical"
+          ? `calc(${weatherStyle.fontSize} * 0.75)`
+          : `calc(${weatherStyle.fontSize} * 0.85)`)
+      : undefined
+  };
+
+  const classicBaseStyle = {
+    ...baseTextStyle,
+    fontSize: weatherStyle.fontSize || undefined
+  };
+
   const iconStyle = {
     width: weatherStyle.iconSize || undefined,
     height: weatherStyle.iconSize || undefined
@@ -66,25 +96,27 @@ function Widget({ options }) {
     return (
       <Container options={options} additionalClassNames={containerClasses}>
         <Raw>
-          <div className="flex flex-row items-center justify-between w-full gap-4" style={textStyle}>
+          <div className="flex flex-row items-center justify-center w-full gap-4" style={baseTextStyle}>
             {/* Left column: Icon and Description below */}
-            <div className="flex flex-col items-center text-center justify-center shrink-0">
+            <div className="flex flex-col items-center text-center justify-center flex-1">
               <WidgetIcon icon={weatherIcon} size="xl" style={iconStyle} />
-              <span 
-                className="secondary-text text-[11px] mt-1 opacity-80 leading-tight block max-w-[90px]"
-                style={{ fontSize: weatherStyle.fontSize ? `calc(${weatherStyle.fontSize} * 0.85)` : undefined }}
-              >
-                {descText}
-              </span>
+              {!weatherStyle.hideDescription && (
+                <span 
+                  className="secondary-text text-[11px] mt-1 opacity-80 leading-tight block max-w-[90px]"
+                  style={descStyle}
+                >
+                  {descText}
+                </span>
+              )}
             </div>
             {/* Right column: Temp and City below */}
-            <div className="flex flex-col text-right justify-center grow">
-              <span className="primary-text text-xl font-bold leading-none" style={textStyle}>
+            <div className="flex flex-col items-center text-center justify-center flex-1">
+              <span className="primary-text text-xl font-bold leading-none whitespace-nowrap" style={tempStyle}>
                 {tempText}
               </span>
               <span 
                 className="secondary-text text-xs mt-1 opacity-70 leading-tight block font-medium"
-                style={{ fontSize: weatherStyle.fontSize ? `calc(${weatherStyle.fontSize} * 0.85)` : undefined }}
+                style={cityStyle}
               >
                 {cityName}
               </span>
@@ -99,21 +131,23 @@ function Widget({ options }) {
     return (
       <Container options={options} additionalClassNames={containerClasses}>
         <Raw>
-          <div className="flex flex-col items-center justify-center w-full text-center" style={textStyle}>
+          <div className="flex flex-col items-center justify-center w-full text-center" style={baseTextStyle}>
             <WidgetIcon icon={weatherIcon} size="xl" style={iconStyle} />
-            <span className="primary-text text-lg font-bold leading-tight mt-1" style={textStyle}>
+            <span className="primary-text text-lg font-bold leading-tight mt-1 whitespace-nowrap" style={tempStyle}>
               {tempText}
             </span>
-            <span 
-              className="secondary-text text-xs opacity-90 leading-tight mt-0.5"
-              style={{ fontSize: weatherStyle.fontSize ? `calc(${weatherStyle.fontSize} * 0.85)` : undefined }}
-            >
-              {descText}
-            </span>
+            {!weatherStyle.hideDescription && (
+              <span 
+                className="secondary-text text-xs opacity-90 leading-tight mt-0.5"
+                style={descStyle}
+              >
+                {descText}
+              </span>
+            )}
             {cityName && (
               <span 
                 className="secondary-text text-[10px] opacity-70 leading-tight mt-1 font-medium"
-                style={{ fontSize: weatherStyle.fontSize ? `calc(${weatherStyle.fontSize} * 0.75)` : undefined }}
+                style={cityStyle}
               >
                 {cityName}
               </span>
@@ -128,16 +162,18 @@ function Widget({ options }) {
   return (
     <Container options={options} additionalClassNames={containerClasses}>
       <PrimaryText>
-        <span style={textStyle}>
+        <span style={classicBaseStyle}>
           {cityName && `${cityName}, `}
-          {tempText}
+          <span className="whitespace-nowrap">{tempText}</span>
         </span>
       </PrimaryText>
-      <SecondaryText>
-        <span style={{ ...textStyle, fontSize: weatherStyle.fontSize ? `calc(${weatherStyle.fontSize} * 0.85)` : undefined }}>
-          {descText}
-        </span>
-      </SecondaryText>
+      {!weatherStyle.hideDescription && (
+        <SecondaryText>
+          <span style={descStyle}>
+            {descText}
+          </span>
+        </SecondaryText>
+      )}
       <WidgetIcon icon={weatherIcon} size="xl" style={iconStyle} />
     </Container>
   );
diff --git a/src/components/widgets/weather/weather.jsx b/src/components/widgets/weather/weather.jsx
index 0e56918..ab84312 100644
--- a/src/components/widgets/weather/weather.jsx
+++ b/src/components/widgets/weather/weather.jsx
@@ -52,11 +52,41 @@ function Widget({ options }) {
     weatherStyle.hideBackground ? "weather-hide-background" : ""
   ].filter(Boolean).join(" ");
 
-  const textStyle = {
-    fontSize: weatherStyle.fontSize || undefined,
+  const baseTextStyle = {
+    fontFamily: weatherStyle.fontFamily || undefined,
     color: weatherStyle.textColor || undefined
   };
 
+  const tempStyle = {
+    ...baseTextStyle,
+    fontSize: weatherStyle.fontSize
+      ? (layout === "custom"
+          ? `calc(${weatherStyle.fontSize} * 1.4)`
+          : layout === "vertical"
+            ? `calc(${weatherStyle.fontSize} * 1.25)`
+            : weatherStyle.fontSize)
+      : undefined
+  };
+
+  const descStyle = {
+    ...baseTextStyle,
+    fontSize: weatherStyle.fontSize ? `calc(${weatherStyle.fontSize} * 0.85)` : undefined
+  };
+
+  const cityStyle = {
+    ...baseTextStyle,
+    fontSize: weatherStyle.fontSize
+      ? (layout === "vertical"
+          ? `calc(${weatherStyle.fontSize} * 0.75)`
+          : `calc(${weatherStyle.fontSize} * 0.85)`)
+      : undefined
+  };
+
+  const classicBaseStyle = {
+    ...baseTextStyle,
+    fontSize: weatherStyle.fontSize || undefined
+  };
+
   const iconStyle = {
     width: weatherStyle.iconSize || undefined,
     height: weatherStyle.iconSize || undefined
@@ -66,25 +96,27 @@ function Widget({ options }) {
     return (
       <Container options={options} additionalClassNames={containerClasses}>
         <Raw>
-          <div className="flex flex-row items-center justify-between w-full gap-4" style={textStyle}>
+          <div className="flex flex-row items-center justify-center w-full gap-4" style={baseTextStyle}>
             {/* Left column: Icon and Description below */}
-            <div className="flex flex-col items-center text-center justify-center shrink-0">
+            <div className="flex flex-col items-center text-center justify-center flex-1">
               <WidgetIcon icon={weatherIcon} size="xl" style={iconStyle} />
-              <span 
-                className="secondary-text text-[11px] mt-1 opacity-80 leading-tight block max-w-[90px]"
-                style={{ fontSize: weatherStyle.fontSize ? `calc(${weatherStyle.fontSize} * 0.85)` : undefined }}
-              >
-                {descText}
-              </span>
+              {!weatherStyle.hideDescription && (
+                <span 
+                  className="secondary-text text-[11px] mt-1 opacity-80 leading-tight block max-w-[90px]"
+                  style={descStyle}
+                >
+                  {descText}
+                </span>
+              )}
             </div>
             {/* Right column: Temp and City below */}
-            <div className="flex flex-col text-right justify-center grow">
-              <span className="primary-text text-xl font-bold leading-none" style={textStyle}>
+            <div className="flex flex-col items-center text-center justify-center flex-1">
+              <span className="primary-text text-xl font-bold leading-none whitespace-nowrap" style={tempStyle}>
                 {tempText}
               </span>
               <span 
                 className="secondary-text text-xs mt-1 opacity-70 leading-tight block font-medium"
-                style={{ fontSize: weatherStyle.fontSize ? `calc(${weatherStyle.fontSize} * 0.85)` : undefined }}
+                style={cityStyle}
               >
                 {cityName}
               </span>
@@ -99,21 +131,23 @@ function Widget({ options }) {
     return (
       <Container options={options} additionalClassNames={containerClasses}>
         <Raw>
-          <div className="flex flex-col items-center justify-center w-full text-center" style={textStyle}>
+          <div className="flex flex-col items-center justify-center w-full text-center" style={baseTextStyle}>
             <WidgetIcon icon={weatherIcon} size="xl" style={iconStyle} />
-            <span className="primary-text text-lg font-bold leading-tight mt-1" style={textStyle}>
+            <span className="primary-text text-lg font-bold leading-tight mt-1 whitespace-nowrap" style={tempStyle}>
               {tempText}
             </span>
-            <span 
-              className="secondary-text text-xs opacity-90 leading-tight mt-0.5"
-              style={{ fontSize: weatherStyle.fontSize ? `calc(${weatherStyle.fontSize} * 0.85)` : undefined }}
-            >
-              {descText}
-            </span>
+            {!weatherStyle.hideDescription && (
+              <span 
+                className="secondary-text text-xs opacity-90 leading-tight mt-0.5"
+                style={descStyle}
+              >
+                {descText}
+              </span>
+            )}
             {cityName && (
               <span 
                 className="secondary-text text-[10px] opacity-70 leading-tight mt-1 font-medium"
-                style={{ fontSize: weatherStyle.fontSize ? `calc(${weatherStyle.fontSize} * 0.75)` : undefined }}
+                style={cityStyle}
               >
                 {cityName}
               </span>
@@ -128,16 +162,18 @@ function Widget({ options }) {
   return (
     <Container options={options} additionalClassNames={containerClasses}>
       <PrimaryText>
-        <span style={textStyle}>
+        <span style={classicBaseStyle}>
           {cityName && `${cityName}, `}
-          {tempText}
+          <span className="whitespace-nowrap">{tempText}</span>
         </span>
       </PrimaryText>
-      <SecondaryText>
-        <span style={{ ...textStyle, fontSize: weatherStyle.fontSize ? `calc(${weatherStyle.fontSize} * 0.85)` : undefined }}>
-          {descText}
-        </span>
-      </SecondaryText>
+      {!weatherStyle.hideDescription && (
+        <SecondaryText>
+          <span style={descStyle}>
+            {descText}
+          </span>
+        </SecondaryText>
+      )}
       <WidgetIcon icon={weatherIcon} size="xl" style={iconStyle} />
     </Container>
   );
diff --git a/src/pages/api/config/[path].js b/src/pages/api/config/[path].js
index b69ddff..c74277d 100644
--- a/src/pages/api/config/[path].js
+++ b/src/pages/api/config/[path].js
@@ -26,6 +26,7 @@ export default async function handler(req, res) {
     // hard-coded since we only support two known files for now
     const mimeType = relativePath === "custom.css" ? "text/css" : "text/javascript";
     res.setHeader("Content-Type", mimeType);
+    res.setHeader("Cache-Control", "no-cache, no-store, must-revalidate");
     return res.status(200).send(fileContent);
   } catch (error) {
     if (error) logger.error(error);
diff --git a/src/pages/index.jsx b/src/pages/index.jsx
index 81a957a..b47113e 100644
--- a/src/pages/index.jsx
+++ b/src/pages/index.jsx
@@ -431,11 +431,13 @@ function Home({ initialSettings }) {
         {tabs.length > 0 && (
           <div key="tabs" id="tabs" className="m-5 sm:m-9 sm:mt-4 sm:mb-0">
             <ul
+              suppressHydrationWarning={true}
               className={classNames(
                 "sm:flex rounded-md",
-                (!settings.pageStyles?.borderStyle ||
+                ((!settings.pageStyles?.borderStyle ||
                   settings.pageStyles?.borderStyle === "none" ||
-                  settings.pageStyles?.borderStyle === "outline")
+                  settings.pageStyles?.borderStyle === "outline") &&
+                  !settings.pageStyles?.hideTabBackground)
                   ? "bg-theme-100/20 dark:bg-white/5"
                   : "bg-transparent",
                 settings.pageStyles?.align === "center" && "sm:justify-center",
@@ -543,7 +545,7 @@ function Home({ initialSettings }) {
         <link rel="preconnect" href="https://fonts.gstatic.com" crossOrigin="anonymous" />
         <link href="https://fonts.googleapis.com/css2?family=Comfortaa:wght@300..700&family=Inter:wght@100..900&family=Outfit:wght@100..900&family=Roboto:ital,wght@0,100..900;1,100..900&display=swap" rel="stylesheet" />
 
-        <style>{`
+        <style suppressHydrationWarning={true}>{`
           #tabs ul li button.tab-style-pill {
             border-radius: 9999px !important;
           }
@@ -579,14 +581,14 @@ function Home({ initialSettings }) {
             content: "" !important;
             display: block !important;
             position: absolute !important;
-            bottom: 2px !important;
-            left: 12.5% !important;
-            width: 75% !important;
-            height: 3px !important;
-            border-radius: 9999px !important;
+            bottom: 1px !important;
+            left: 15% !important;
+            width: 70% !important;
+            height: 4px !important;
+            border-radius: 2px !important;
             background-color: transparent !important;
             z-index: 10 !important;
-            transition: all 0.2s ease-in-out !important;
+            transition: transform 0.25s cubic-bezier(0.4, 0, 0.2, 1), background-color 0.2s ease-in-out !important;
             transform: scaleX(0) !important;
           }
           #tabs ul li button.tab-style-underline-rounded[aria-selected="true"]::after {
@@ -601,6 +603,15 @@ function Home({ initialSettings }) {
             transform: scaleX(1) !important;
           }
           
+          ${settings.pageStyles?.hideTabBackground ? `
+            #tabs ul {
+              background-color: transparent !important;
+              background: transparent !important;
+              border-color: transparent !important;
+              box-shadow: none !important;
+            }
+          ` : ''}
+          
           ${settings.pageStyles?.fontFamily ? `
             #tabs ul, #tabs ul li button, #tabs ul li button span {
               font-family: ${settings.pageStyles.fontFamily.includes(' ') || settings.pageStyles.fontFamily.includes('-') ? `'${settings.pageStyles.fontFamily}'` : settings.pageStyles.fontFamily} !important;
diff --git a/src/widgets/components.js b/src/widgets/components.js
index fbc34d3..fd4ca1a 100644
--- a/src/widgets/components.js
+++ b/src/widgets/components.js
@@ -3,6 +3,7 @@ import dynamic from "next/dynamic";
 const components = {
   adguard: dynamic(() => import("./adguard/component")),
   apcups: dynamic(() => import("./apcups/component")),
+  antigravity: dynamic(() => import("./antigravity/component")),
   arcane: dynamic(() => import("./arcane/component")),
   argocd: dynamic(() => import("./argocd/component")),
   atsumeru: dynamic(() => import("./atsumeru/component")),
@@ -22,6 +23,7 @@ const components = {
   checkmk: dynamic(() => import("./checkmk/component")),
   cloudflared: dynamic(() => import("./cloudflared/component")),
   coinmarketcap: dynamic(() => import("./coinmarketcap/component")),
+  codex: dynamic(() => import("./codex/component")),
   crowdsec: dynamic(() => import("./crowdsec/component")),
   iframe: dynamic(() => import("./iframe/component")),
   customapi: dynamic(() => import("./customapi/component")),
diff --git a/src/widgets/widgets.js b/src/widgets/widgets.js
index 7f18efa..ae04c97 100644
--- a/src/widgets/widgets.js
+++ b/src/widgets/widgets.js
@@ -1,5 +1,6 @@
 import adguard from "./adguard/widget";
 import apcups from "./apcups/widget";
+import antigravity from "./antigravity/widget";
 import arcane from "./arcane/widget";
 import argocd from "./argocd/widget";
 import atsumeru from "./atsumeru/widget";
@@ -19,6 +20,7 @@ import channelsdvrserver from "./channelsdvrserver/widget";
 import checkmk from "./checkmk/widget";
 import cloudflared from "./cloudflared/widget";
 import coinmarketcap from "./coinmarketcap/widget";
+import codex from "./codex/widget";
 import crowdsec from "./crowdsec/widget";
 import customapi from "./customapi/widget";
 import deluge from "./deluge/widget";
@@ -156,6 +158,7 @@ import zabbix from "./zabbix/widget";
 const widgets = {
   adguard,
   apcups,
+  antigravity,
   arcane,
   argocd,
   atsumeru,
@@ -174,6 +177,7 @@ const widgets = {
   checkmk,
   cloudflared,
   coinmarketcap,
+  codex,
   crowdsec,
   customapi,
   deluge,
