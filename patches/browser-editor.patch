diff --git a/next.config.js b/next.config.js
index ea49bf54..87c163d3 100644
--- a/next.config.js
+++ b/next.config.js
@@ -1,9 +1,18 @@
 const { i18n } = require("./next-i18next.config");
 
+const allowedDevOrigins = (process.env.HOMEPAGE_ALLOWED_DEV_ORIGINS ?? "")
+  .split(",")
+  .map((origin) => origin.trim())
+  .filter(Boolean);
+
 /** @type {import('next').NextConfig} */
 const nextConfig = {
   reactStrictMode: true,
   output: "standalone",
+  env: {
+    HOMEPAGE_BROWSER_EDITOR: process.env.HOMEPAGE_BROWSER_EDITOR ?? "",
+  },
+  ...(allowedDevOrigins.length ? { allowedDevOrigins } : {}),
   images: {
     remotePatterns: [
       {
diff --git a/src/components/bookmarks/group.jsx b/src/components/bookmarks/group.jsx
index 88d4d7e8..c44b9eae 100644
--- a/src/components/bookmarks/group.jsx
+++ b/src/components/bookmarks/group.jsx
@@ -5,6 +5,7 @@ import ErrorBoundary from "components/errorboundry";
 import ResolvedIcon from "components/resolvedicon";
 import { useEffect, useRef } from "react";
 import { MdKeyboardArrowDown } from "react-icons/md";
+import { EditorGroupToolbar, useConfigEditor } from "mods/browser-editor/components/editor";
 
 export default function BookmarksGroup({
   bookmarks,
@@ -14,12 +15,16 @@ export default function BookmarksGroup({
   bookmarksStyle,
   maxGroupColumns,
 }) {
+  const { editMode } = useConfigEditor();
   const panel = useRef();
-
   useEffect(() => {
     if (layout?.initiallyCollapsed ?? groupsInitiallyCollapsed) panel.current.style.height = `0`;
   }, [layout, groupsInitiallyCollapsed]);
 
+  if (!editMode && (bookmarks.bookmarks?.length ?? 0) === 0) {
+    return null;
+  }
+
   return (
     <div
       key={bookmarks.name}
@@ -35,6 +40,7 @@ export default function BookmarksGroup({
       <Disclosure defaultOpen={!(layout?.initiallyCollapsed ?? groupsInitiallyCollapsed)}>
         {({ open }) => (
           <>
+            <EditorGroupToolbar type="bookmarks" groupName={bookmarks.name} layout={layout} />
             {layout?.header !== false && (
               <Disclosure.Button disabled={disableCollapse} className="flex w-full select-none items-center group">
                 {layout?.icon && (
@@ -73,7 +79,12 @@ export default function BookmarksGroup({
             >
               <Disclosure.Panel className="transition-all overflow-hidden duration-300 ease-out" ref={panel} static>
                 <ErrorBoundary>
-                  <List bookmarks={bookmarks.bookmarks} layout={layout} bookmarksStyle={bookmarksStyle} />
+                  <List
+                    groupName={bookmarks.name}
+                    bookmarks={bookmarks.bookmarks}
+                    layout={layout}
+                    bookmarksStyle={bookmarksStyle}
+                  />
                 </ErrorBoundary>
               </Disclosure.Panel>
             </Transition>
diff --git a/src/components/bookmarks/item.jsx b/src/components/bookmarks/item.jsx
index 7ea16bba..1a9c3d4c 100644
--- a/src/components/bookmarks/item.jsx
+++ b/src/components/bookmarks/item.jsx
@@ -1,10 +1,24 @@
 import classNames from "classnames";
+import { useEditableItem } from "mods/browser-editor/components/editor";
 import ResolvedIcon from "components/resolvedicon";
 import { useContext } from "react";
 import { SettingsContext } from "utils/contexts/settings";
 
+function bookmarkDescription(bookmark) {
+  if (bookmark.description) {
+    return bookmark.description;
+  }
+
+  try {
+    return new URL(bookmark.href).hostname;
+  } catch {
+    return bookmark.href ?? "";
+  }
+}
+
 export default function Item({ bookmark, iconOnly = false }) {
-  const description = bookmark.description ?? new URL(bookmark.href).hostname;
+  const description = bookmarkDescription(bookmark);
+  const { editMode, itemProps } = useEditableItem("bookmarks", bookmark.groupName, bookmark.name, bookmark);
   const { settings } = useContext(SettingsContext);
 
   return (
@@ -15,11 +29,14 @@ export default function Item({ bookmark, iconOnly = false }) {
       data-name={bookmark.name}
     >
       <a
-        href={bookmark.href}
+        href={editMode ? undefined : bookmark.href}
         title={bookmark.name}
         rel="noreferrer"
-        target={bookmark.target ?? settings.target ?? "_blank"}
+        target={editMode ? undefined : bookmark.target ?? settings.target ?? "_blank"}
+        {...itemProps}
         className={classNames(
+          "relative",
+          editMode && "border-2 border-dashed border-theme-400/70 dark:border-white/25",
           settings.cardBlur !== undefined && `backdrop-blur${settings.cardBlur.length ? "-" : ""}${settings.cardBlur}`,
           "text-left cursor-pointer transition-all rounded-md font-medium text-theme-700 dark:text-theme-200 dark:hover:text-theme-300 shadow-md shadow-theme-900/10 dark:shadow-theme-900/20 bg-theme-100/20 hover:bg-theme-300/20 dark:bg-white/5 dark:hover:bg-white/10",
           iconOnly ? "h-[60px] w-[60px] grid" : "block w-full mb-3",
diff --git a/src/components/bookmarks/list.jsx b/src/components/bookmarks/list.jsx
index f24794db..7e34298f 100644
--- a/src/components/bookmarks/list.jsx
+++ b/src/components/bookmarks/list.jsx
@@ -1,9 +1,10 @@
 import classNames from "classnames";
 import Item from "components/bookmarks/item";
+import { EditorAddTile } from "mods/browser-editor/components/editor";
 
 import { columnMap } from "../../utils/layout/columns";
 
-export default function List({ bookmarks, layout, bookmarksStyle }) {
+export default function List({ groupName, bookmarks, layout, bookmarksStyle }) {
   let classes = layout?.style === "row" ? `grid ${columnMap[layout?.columns]} gap-x-2` : "flex flex-col bookmark-list";
   const style = {};
   if (layout?.iconsOnly || bookmarksStyle === "icons") {
@@ -15,10 +16,20 @@ export default function List({ bookmarks, layout, bookmarksStyle }) {
       {bookmarks.map((bookmark) => (
         <Item
           key={`${bookmark.name}-${bookmark.href}`}
-          bookmark={bookmark}
+          bookmark={{ ...bookmark, groupName }}
           iconOnly={layout?.iconsOnly || bookmarksStyle === "icons"}
         />
       ))}
+      <EditorAddTile
+        type="bookmarks"
+        groupName={groupName}
+        label="Add"
+        wrapperClassName={classNames("bookmark", (layout?.iconsOnly || bookmarksStyle === "icons") && "grid")}
+        className={classNames(
+          "flex items-center justify-center rounded-md border-2 border-dashed border-theme-400/70 bg-theme-100/20 p-3 text-sm font-medium text-theme-700 shadow-md shadow-theme-900/10 hover:bg-theme-300/20 dark:border-white/20 dark:bg-white/5 dark:text-theme-200 dark:hover:bg-white/10",
+          layout?.iconsOnly || bookmarksStyle === "icons" ? "h-[60px] w-[60px]" : "mb-3 min-h-12 w-full",
+        )}
+      />
     </ul>
   );
 }
diff --git a/src/components/services/group.jsx b/src/components/services/group.jsx
index 403f6063..16f5a0df 100644
--- a/src/components/services/group.jsx
+++ b/src/components/services/group.jsx
@@ -4,6 +4,7 @@ import ResolvedIcon from "components/resolvedicon";
 import List from "components/services/list";
 import { useEffect, useRef } from "react";
 import { MdKeyboardArrowDown } from "react-icons/md";
+import { EditorGroupToolbar, useConfigEditor } from "mods/browser-editor/components/editor";
 
 import { columnMap } from "../../utils/layout/columns";
 
@@ -16,18 +17,47 @@ export default function ServicesGroup({
   groupsInitiallyCollapsed,
   isSubgroup,
 }) {
+  const { editMode } = useConfigEditor();
   const panel = useRef();
-
   useEffect(() => {
     if (layout?.initiallyCollapsed ?? groupsInitiallyCollapsed) panel.current.style.height = `0`;
   }, [layout, groupsInitiallyCollapsed]);
 
+  const subgroupsFromApi = group.groups ?? [];
+  const subgroupNamesFromApi = new Set(subgroupsFromApi.map((subgroup) => subgroup.name));
+  const layoutSubgroups = Object.entries(layout ?? {})
+    .filter(
+      ([key, value]) =>
+        ![
+          "style",
+          "columns",
+          "header",
+          "tab",
+          "icon",
+          "initiallyCollapsed",
+          "useEqualHeights",
+          "alignRowHeights",
+        ].includes(key),
+    )
+    .filter(([, value]) => value && typeof value === "object" && !Array.isArray(value))
+    .map(([key]) => ({ name: key, services: [], groups: [] }))
+    .filter((subgroup) => !subgroupNamesFromApi.has(subgroup.name));
+  const visibleSubgroups = [...subgroupsFromApi, ...(editMode ? layoutSubgroups : [])];
+  const isEmptyGroup = (group.services?.length ?? 0) === 0 && visibleSubgroups.length === 0;
+
+  if (!editMode && isEmptyGroup) {
+    return null;
+  }
+
   let groupPadding = layout?.header === false ? "px-1" : "p-1 pb-0";
   if (isSubgroup) groupPadding = "";
 
   return (
     <div
       key={group.name}
+      data-editor-service-group="true"
+      data-editor-group-name={group.name}
+      data-editor-align-row-heights={layout?.alignRowHeights === false ? "false" : "true"}
       className={classNames(
         "services-group flex-1",
         layout?.style === "row" ? "basis-full" : "basis-full md:basis-1/2 lg:basis-1/3 xl:basis-1/4",
@@ -39,6 +69,7 @@ export default function ServicesGroup({
       <Disclosure defaultOpen={!(layout?.initiallyCollapsed ?? groupsInitiallyCollapsed)}>
         {({ open }) => (
           <>
+            <EditorGroupToolbar type="services" groupName={group.name} layout={layout} allowInside />
             {layout?.header !== false && (
               <Disclosure.Button disabled={disableCollapse} className="flex w-full select-none items-center group">
                 {layout?.icon && (
@@ -86,13 +117,13 @@ export default function ServicesGroup({
                   useEqualHeights={useEqualHeights}
                   header={layout?.header !== false}
                 />
-                {group.groups?.length > 0 && (
+                {visibleSubgroups.length > 0 && (
                   <div
                     className={`grid ${
                       layout?.style === "row" ? `grid ${columnMap[layout?.columns]} gap-x-2` : "flex flex-col"
                     } gap-2`}
                   >
-                    {group.groups.map((subgroup) => (
+                    {visibleSubgroups.map((subgroup) => (
                       <ServicesGroup
                         key={subgroup.name}
                         group={subgroup}
diff --git a/src/components/services/item.jsx b/src/components/services/item.jsx
index c23e16a2..5abbeb39 100644
--- a/src/components/services/item.jsx
+++ b/src/components/services/item.jsx
@@ -1,4 +1,5 @@
 import classNames from "classnames";
+import { useEditableItem } from "mods/browser-editor/components/editor";
 import ResolvedIcon from "components/resolvedicon";
 import { useContext, useState } from "react";
 import { SettingsContext } from "utils/contexts/settings";
@@ -15,6 +16,7 @@ import Widget from "./widget";
 
 export default function Item({ service, groupName, useEqualHeights }) {
   const hasLink = service.href && service.href !== "#";
+  const { editMode, itemProps } = useEditableItem("services", groupName, service.name, service);
   const { settings } = useContext(SettingsContext);
   const showStats = service.showStats === false ? false : settings.showStats;
   const statusStyle = service.statusStyle !== undefined ? service.statusStyle : settings.statusStyle;
@@ -188,6 +190,15 @@ export default function Item({ service, groupName, useEqualHeights }) {
         {service.widgets.map((widget) => (
           <Widget widget={widget} service={service} key={widget.index} />
         ))}
+
+        {editMode && (
+          <button
+            type="button"
+            {...itemProps}
+            className="absolute inset-0 z-40 cursor-move rounded-md border-2 border-dashed border-theme-400/70 bg-transparent text-left dark:border-white/25"
+            aria-label={`Edit ${service.name}`}
+          />
+        )}
       </div>
     </li>
   );
diff --git a/src/components/services/list.jsx b/src/components/services/list.jsx
index 1acf94d6..84796ccb 100644
--- a/src/components/services/list.jsx
+++ b/src/components/services/list.jsx
@@ -1,13 +1,17 @@
 import classNames from "classnames";
 import Item from "components/services/item";
+import { EditorAddTile } from "mods/browser-editor/components/editor";
 
 import { columnMap } from "../../utils/layout/columns";
 
 export default function List({ groupName, services, layout, useEqualHeights, header }) {
+  const isRowLayout = layout?.style === "row";
+
   return (
     <ul
+      data-editor-service-list={groupName}
       className={classNames(
-        layout?.style === "row" ? `grid ${columnMap[layout?.columns]} gap-x-2` : "flex flex-col",
+        isRowLayout ? `grid ${columnMap[layout?.columns]} gap-x-2` : "flex flex-col",
         header ? "mt-3" : "",
         "services-list",
       )}
@@ -20,6 +24,13 @@ export default function List({ groupName, services, layout, useEqualHeights, hea
           useEqualHeights={layout?.useEqualHeights ?? useEqualHeights}
         />
       ))}
+      <EditorAddTile
+        type="services"
+        groupName={groupName}
+        label="Add service"
+        wrapperClassName="service"
+        className="mb-2 flex min-h-16 w-full items-center justify-center rounded-md border-2 border-dashed border-theme-400/70 bg-theme-100/20 p-3 text-sm font-medium text-theme-700 shadow-md shadow-theme-900/10 hover:bg-theme-300/20 dark:border-white/20 dark:bg-white/5 dark:text-theme-200 dark:hover:bg-white/10"
+      />
     </ul>
   );
 }
diff --git a/src/pages/index.jsx b/src/pages/index.jsx
index c243c3ff..689eec9c 100644
--- a/src/pages/index.jsx
+++ b/src/pages/index.jsx
@@ -7,6 +7,7 @@ import ServicesGroup from "components/services/group";
 import Tab, { slugifyAndEncode } from "components/tab";
 import Revalidate from "components/toggles/revalidate";
 import Widget from "components/widgets/widget";
+import { ConfigEditorProvider, RootGroupDropZone } from "mods/browser-editor/components/editor";
 import { useTranslation } from "next-i18next";
 import { serverSideTranslations } from "next-i18next/serverSideTranslations";
 import dynamic from "next/dynamic";
@@ -295,9 +296,26 @@ function Home({ initialSettings }) {
   const servicesAndBookmarksGroups = useMemo(() => {
     const tabGroupFilter = (g) => g && [activeTab, ""].includes(slugifyAndEncode(settings.layout?.[g.name]?.tab));
     const undefinedGroupFilter = (g) => settings.layout?.[g.name] === undefined;
+    const serviceMap = new Map((services ?? []).map((group) => [group.name, group]));
+    const bookmarkMap = new Map((bookmarks ?? []).map((group) => [group.name, group]));
+    const layoutEntries = Object.entries(settings.layout ?? {});
+
+    const layoutGroups = layoutEntries
+      .map(([groupName]) => {
+        if (groupName === "Bookmarks") {
+          return null;
+        }
+
+        if (serviceMap.has(groupName)) {
+          return serviceMap.get(groupName);
+        }
 
-    const layoutGroups = Object.keys(settings.layout ?? {})
-      .map((groupName) => services?.find((g) => g.name === groupName) ?? bookmarks?.find((b) => b.name === groupName))
+        if (bookmarkMap.has(groupName)) {
+          return bookmarkMap.get(groupName);
+        }
+
+        return { name: groupName, services: [], groups: [] };
+      })
       .filter(tabGroupFilter);
 
     if (!settings.layout && JSON.stringify(settings.layout) !== JSON.stringify(initialSettings.layout)) {
@@ -305,8 +323,14 @@ function Home({ initialSettings }) {
       return <div />;
     }
 
-    const serviceGroups = services?.filter(tabGroupFilter).filter(undefinedGroupFilter);
-    const bookmarkGroups = bookmarks.filter(tabGroupFilter).filter(undefinedGroupFilter);
+    const missingBookmarkGroups = Object.keys(settings.layout?.Bookmarks ?? {})
+      .filter((groupName) => !bookmarkMap.has(groupName))
+      .map((groupName) => ({ name: groupName, bookmarks: [] }));
+
+    const serviceGroups = [...(services?.filter(tabGroupFilter).filter(undefinedGroupFilter) ?? [])];
+    const bookmarkGroups = [...(bookmarks?.filter(tabGroupFilter).filter(undefinedGroupFilter) ?? []), ...missingBookmarkGroups]
+      .filter(tabGroupFilter)
+      .filter(undefinedGroupFilter);
 
     return (
       <>
@@ -436,12 +460,13 @@ function Home({ initialSettings }) {
 
       <Script src="/api/config/custom.js" />
 
-      <div
-        className={classNames(
-          settings.fullWidth ? "" : "container",
-          "relative m-auto flex flex-col justify-start z-10 h-full min-h-screen",
-        )}
-      >
+      <ConfigEditorProvider>
+        <div
+          className={classNames(
+            settings.fullWidth ? "" : "container",
+            "relative m-auto flex flex-col justify-start z-10 h-full min-h-screen",
+          )}
+        >
         <QuickLaunch
           servicesAndBookmarks={servicesAndBookmarks}
           searchString={searchString}
@@ -494,7 +519,7 @@ function Home({ initialSettings }) {
           </div>
         </div>
 
-        {servicesAndBookmarksGroups}
+        <RootGroupDropZone>{servicesAndBookmarksGroups}</RootGroupDropZone>
 
         <div id="footer" className="flex flex-col mt-auto p-8 w-full">
           <div id="style" className="flex w-full justify-end">
@@ -507,7 +532,8 @@ function Home({ initialSettings }) {
             {!settings.hideVersion && <Version disableUpdateCheck={settings.disableUpdateCheck} />}
           </div>
         </div>
-      </div>
+        </div>
+      </ConfigEditorProvider>
     </>
   );
 }
